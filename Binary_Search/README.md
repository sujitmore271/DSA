# Binary Search - Easy

1. **Lower Bound**:
  - **logic**: - Use binary search  If nums[mid] >= target then ans = mid high = mid - 1 or Else: low = mid + 1
  - **complexity**: O (log n)
   --------------------------------